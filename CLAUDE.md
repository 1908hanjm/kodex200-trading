# CLAUDE.md — kodex200-trading 프로젝트 작업 규칙

이 문서는 이 저장소에서 작업할 때 항상 적용되는 규칙입니다. 별도 지시가 없어도 아래 원칙을 따르세요.

## 프로젝트 개요
- KODEX200(069500) ETF 자동매매 시스템, C#/.NET, XingAPI(대신증권) 기반
- 10-band 그리드 매매 구조: 가격이 오르내리며 각 밴드가 독립적으로 매수/매도 조건을 판정
- 실거래 계좌에 연결되는 프로그램입니다. 여기서 발생하는 버그는 실제 금전 손실로 이어질 수 있습니다.

## 절대 규칙 — 작업 범위

1. **지시받은 파일만 수정한다.** 사용자가 명시적으로 언급하지 않은 파일은 새로 만들거나 수정하지 않는다.
   - 특히 다음 핵심 파일들은 지시에 명시적으로 포함되지 않는 한 절대 건드리지 않는다:
     `0250_Tick_Process.cs`, `0270_틱계산2.cs`, `0300_BUY판정.cs`, `밴드매칭` 관련 파일,
     `0500_매매전송_Xing.cs`, `0530_매매_Xing.cs`, `체결확인` 관련 파일, `번호_매핑` 관련 파일
   - `.github/copilot-instructions.md` 등 작업과 무관한 설정 파일을 임의로 만들지 않는다.

2. **한 커밋 = 한 가지 목적.** 여러 이슈를 한 커밋에 섞지 않는다. 커밋 메시지는 실제로 무엇을 고쳤는지 정확히 서술한다.

3. **변경 규모가 예상보다 크면 멈추고 물어본다.** 지시한 범위에 비해 비정상적으로 많은 줄이 바뀌었다면
   (예: 파일 하나에서 500줄 이상, 또는 지시하지 않은 파일이 포함됨) 커밋하지 말고 먼저 사용자에게 이유를 설명하고 확인받는다.

## 절대 규칙 — 커밋 전 확인

4. **커밋하기 전에 항상 `git status`와 `git diff --stat` 결과를 사용자에게 보여주고 승인을 받는다.**
   임의로 커밋하거나 푸시하지 않는다.

5. **작업 완료 후 요약할 때, 실제로 어떤 파일이 왜 바뀌었는지 구체적으로 설명한다.**
   "고쳤습니다" 같은 뭉뚱그린 답변 대신 클래스/메서드 단위로 무엇을 바꿨는지 명시한다.

## 절대 규칙 — 빌드/검증

6. **작업 완료 후 반드시 재빌드(Rebuild)를 안내하고, 빌드 결과물(exe)의 수정 시각이 방금 빌드된 시각인지
   확인하도록 사용자에게 상기시킨다.** (과거 4일 지난 구버전 exe로 실거래를 돌린 사고가 있었음 — 재발 방지)

7. 로그 분석/디버깅 요청 시, 코드를 고치기 전에 먼저 **로그 근거(정확한 라인/타임스탬프)**를 인용해서
   원인을 설명하고, 사용자가 동의한 뒤에 수정을 진행한다. 추측성 수정을 하지 않는다.

## 현재 진행 중인 이슈 (참고용, 최신 상태는 사용자에게 재확인)

- **CASH_GUARD 캐시 미동기화**: `0900.QueryAllAsync`가 CSPAQ12200을 조회해도 그 결과가
  `0500_매매전송_Xing.cs`의 CASH_GUARD 캐시에 반영되지 않아, 직후 매수 시도 시 불필요한 재조회/스로틀/차단이 발생함.
- **CASH_GUARD 재시도 대기시간 부족**: 재시도(1.5초×2회=3초)가 CSPAQ12200의 스로틀 간격(10초)보다 훨씬 짧아
  구조적으로 통과 불가능한 케이스가 존재함.
- 위 두 건에 대한 상세 작업지시서는 별도 파일로 제공됨 (작업지시서_CASH_GUARD_캐시동기화_재시도재설계.md)

## 커뮤니케이션 스타일
- 한국어 존댓말로 응답
- 수정 완료 보고 시 파일명/클래스명/메서드명을 구체적으로 명시
- 확신 없는 부분은 "확인 필요"로 명시하고 추측으로 단정하지 않음
